<%= form_for @measurement, :html => { :class => 'form-horizontal form-model' } do |f| %>
  <div class="form-group">
    <%= f.label :label, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_field :label, :class => 'text-field form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :ts_taken, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.datetime_select :ts_taken, :class => 'text-field form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :device, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_field :device, :class => 'text-field form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class='col-md-offset-2 col-md-10'>
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                  measurements_path, :class => 'btn btn-default' %>
    </div>
  </div>
<% end %>
